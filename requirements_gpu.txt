# WaterPrint GPU Requirements
# For NVIDIA DGX Spark with Blackwell Architecture
#
# Installation:
#   pip install -r requirements_gpu.txt
#
# For CUDA/cuDF (RAPIDS), use conda:
#   conda install -c rapidsai -c conda-forge -c nvidia cudf=24.12 python=3.11 cuda-version=12.0

# Core dependencies (same as CPU)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
shap>=0.42
joblib

# XGBoost with GPU support
# The same package supports both CPU and GPU - GPU is enabled via tree_method='gpu_hist'
xgboost>=2.0

# Optional: RAPIDS cuDF for GPU-accelerated DataFrames
# Install via conda (not pip): conda install -c rapidsai cudf
# cudf>=24.12

# Optional: CuPy for GPU-accelerated NumPy operations
# cupy-cuda12x>=13.0

# Scipy for statistical tests
scipy>=1.11
